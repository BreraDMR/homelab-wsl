# homelab root aggregator.
# `docker compose up -d` here brings up the whole lab.
# Networks are external/shared — run bin/bootstrap.sh once first.
name: homelab

include:
  - infra/compose.yaml
  # --- bots ---
  - bots/pc-tele-monitor-ai/compose.yaml
  # --- sites (add as they go live) ---
  # - sites/example/compose.yaml
