# Replace with your bot's runtime. Example (python):
# FROM python:3.12-slim
# WORKDIR /app
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
# COPY . .
# CMD ["python", "bot.py"]
FROM alpine:3.20
CMD ["echo", "replace this Dockerfile with your bot"]
